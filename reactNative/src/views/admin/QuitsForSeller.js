import { Column } from '../../other/Components/Html'
import s from './Admin.module.scss'

const QuitsForSeller = (p) => {
  return (
    <Column f={1} >QuitsForSeller</Column>
  )
}

export default QuitsForSeller