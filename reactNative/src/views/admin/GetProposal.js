import { Column } from '../../other/Components/Html'
import s from './Admin.module.scss'

const GetProposal = (p) => {
  return (
    <Column f={1} >GetProposal</Column>
  )
}

export default GetProposal