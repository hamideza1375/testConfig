import { Column } from '../../other/Components/Html'
import s from './Admin.module.scss'

const CreateCategory = (p) => {
  return (
    <Column f={1} >CreateCategory</Column>
  )
}

export default CreateCategory