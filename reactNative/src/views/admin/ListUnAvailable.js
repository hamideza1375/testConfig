import { Column } from '../../other/Components/Html'
import s from './Admin.module.scss'

const ListUnAvailable = (p) => {
  return (
    <Column f={1} >ListUnAvailable</Column>
  )
}

export default ListUnAvailable