import { Column } from '../../other/Components/Html'
import s from './Admin.module.scss'

const CreateSlider = (p) => {
  return (
    <Column f={1} >CreateSlider</Column>
  )
}

export default CreateSlider