import { Column } from '../../other/Components/Html'
import s from './client.module.scss'

const ProductsPopulars = (p) => {
  return (
    <Column f={1} >ProductsPopulars</Column>
  )
}

export default ProductsPopulars