import { Column } from '../../other/Components/Html'
import s from './User.module.scss'

const SendProposal = (p) => {
  return (
    <Column f={1} >SendProposal</Column>
  )
}

export default SendProposal