import { Column } from '../../other/Components/Html'
import s from './User.module.scss'

const ResetSpecification = (p) => {
  return (
    <Column f={1} >ResetSpecification</Column>
  )
}

export default ResetSpecification