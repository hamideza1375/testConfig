import { Column } from '../../other/Components/Html'
import s from './User.module.scss'

const ActiveOrder = (p) => {
  return (
    <Column f={1} >ActiveOrder</Column>
  )
}

export default ActiveOrder