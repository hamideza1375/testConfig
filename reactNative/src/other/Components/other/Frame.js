
import React from 'react'
import { WebView } from 'react-native-webview'

const Frame = (props) => {
    return (
        <>
            <WebView {...props} />
        </>
    )
}

export default Frame